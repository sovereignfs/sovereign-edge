{
  "name": "sovereign-edge",
  "version": "0.2.1",
  "private": true,
  "description": "Sovereign Edge — a privacy-first, fully offline on-device AI companion. Workspace root: apps/mobile (shipping), apps/desktop (Tauri v2, epic 12 not started), packages/* (shared, unpublished).",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "start": "pnpm --filter mobile start",
    "ios": "pnpm --filter mobile ios",
    "android": "pnpm --filter mobile android",
    "desktop": "pnpm --filter desktop dev",
    "prebuild": "pnpm --filter mobile prebuild",
    "test": "pnpm -r --if-present test",
    "test:watch": "pnpm --filter mobile test:watch",
    "typecheck": "pnpm -r --if-present typecheck",
    "lint": "pnpm -r --if-present lint",
    "lint:fix": "pnpm -r --if-present lint:fix",
    "check:offline": "pnpm -r --if-present check:offline",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.5.0"
  },
  "packageManager": "pnpm@11.5.2",
  "engines": {
    "node": ">=24"
  }
}
