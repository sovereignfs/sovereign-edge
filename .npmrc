# React Native's Metro bundler and the autolinking in expo prebuild both
# resolve native modules by walking node_modules, which pnpm's symlinked
# layout breaks. Hoisting is the configuration Expo documents for pnpm.
node-linker=hoisted
