/**
 * Task 12.1's own scope: an empty window, nothing more. The chat UI is task
 * 12.7's job, once 12.2 (inference) and 12.4 (connector framework) exist for
 * it to actually drive — see docs/epics/desktop/core-port.md.
 */
export function App() {
  return (
    <main>
      <h1>Sovereign Edge</h1>
      <p>Desktop scaffold — task 12.1. No product features yet.</p>
    </main>
  );
}
