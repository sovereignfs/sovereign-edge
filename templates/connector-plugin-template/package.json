{
  "name": "my-sovereign-connector",
  "version": "0.1.0",
  "private": true,
  "description": "Starting point for a Sovereign Edge connector — clone this, edit manifest.json, run the validator.",
  "scripts": {
    "validate": "node validate.mjs"
  },
  "devDependencies": {
    "@sovereignfs/connector-sdk": "^0.1.0"
  }
}
